{
  "name": "@ember/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ember's local API — seeded catalogue, orders and reviews, served over HTTP.",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
