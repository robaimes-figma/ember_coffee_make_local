packages:
  - 'apps/*'
  - 'packages/*'

# esbuild ships a prebuilt binary and needs its install script to run.
# Declared explicitly so a fresh clone never stops to ask.
allowBuilds:
  esbuild: true
